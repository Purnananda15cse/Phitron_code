#include <bits/stdc++.h>
using namespace std;
int main()
{
    char a;
    a = getchar(); //getline can take whole line as input
     //so when there is need to take input name and other int data have to take input from next line
    cout << a << endl;

    return 0;
}
